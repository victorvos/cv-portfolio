---
trigger: glob
globs: "**/*.py"
---

# Python Development Guidelines

**Canonical source:** `.cursor/rules/python.mdc` (and `.cursor/rules/langchain_python.mdc` for LangChain).

Use those files for FastAPI, clean architecture (darom: core/, infrastructure/, presentation/), services, DI, and code style. This file is a pointer only.
